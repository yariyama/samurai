using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DemoMainUIManager : MonoBehaviour
{
    //ÉÇÅ[ÉhNO
    public int _mode_st;

    // Start is called before the first frame update
    void Start()
    {
        _mode_st = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
